import React from "react";
import { useQuery } from '@apollo/client';

const [loading, data] = useQuery()

console.log(data);

function Main() {
    return (
        <>
            
        </>
    )
}

export default Main;